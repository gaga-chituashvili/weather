# React + TypeScript + Vite Template

A minimal **React + TypeScript + Vite** template with HMR, ESLint, and Tailwind-ready setup.  
Designed for **fast development and type-safe code**.

---

## Features

- React 18 + TypeScript
- Vite for fast builds and HMR
- ESLint preconfigured for type-aware linting
- Optional React Compiler support
- Tailwind CSS ready (if added)
- Modular and easy-to-expand configuration

📁 Project Structure
```
├── README.md                     # Project documentation
├── dist/                         # Production build output (generated by Vite)
│   ├── assets/                   # Optimized and hashed static assets
│   │   ├── index-DOnXRUjB.css     # Compiled and minified CSS
│   │   ├── index-DQaze3cM.js      # Compiled and minified JavaScript
│   │   ├── logo-hk6Dn71M.svg      # Optimized logo asset
│   │   └── weather-BsLZqPfN.svg   # Weather icon asset
│   └── index.html                # Production HTML entry point
│
├── eslint.config.js               # ESLint configuration
├── index.html                     # HTML template (Vite entry)
├── package.json                   # Project metadata and dependencies
├── package-lock.json              # Locked dependency versions
├── postcss.config.cjs             # PostCSS configuration
│
├── public/                        # Public static files (served as-is)
│
├── src/                           # Application source code
│   ├── App.tsx                    # Root React component
│   ├── App.css                    # Global application styles
│   ├── index.css                  # Tailwind base styles
│   ├── main.tsx                   # Application entry point
│
│   ├── assets/                    # Source assets (icons, images)
│   │   ├── logo.svg
│   │   └── weather.svg
│
│   ├── components/                # Reusable UI components
│   │   ├── DailyForecast.tsx      # Daily weather forecast component
│   │   ├── HourlyForecast.tsx     # Hourly forecast component
│   │   ├── MainCard.tsx           # Main weather display card
│   │   ├── SearchBar.tsx          # City search input
│   │   ├── StatsGrid.tsx          # Weather statistics grid
│   │   ├── TopBar.tsx             # Header / navigation bar
│   │   └── Weather.tsx            # Core weather logic component
│
│   └── context/
│       └── UnitsContext.tsx       # Context for temperature units (°C / °F)
│
├── tailwind.config.cjs            # Tailwind CSS configuration
├── tsconfig.json                  # Base TypeScript configuration
├── tsconfig.app.json              # TypeScript config for the app
├── tsconfig.node.json             # TypeScript config for Node tooling
├── vite.config.ts                 # Vite build & dev server configuration
└── weather                        # Project identifier / environment marker
```

## Installation

```bash
# Clone the repository
git clone https://github.com/gaga-chituashvili/weather.git
cd weather

# Install dependencies
npm install

# Start development server
npm run dev

# Build for production
npm run build
